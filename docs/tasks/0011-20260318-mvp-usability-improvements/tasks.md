# TASKS-0011: MVPユーザビリティ改善（判定UI・開始トリガー・体格補正）

- **Date**: 2026-03-18
- **Owner**: @tujing
- **Related PLAN**: [PLAN-0011](docs/tasks/0011-20260318-mvp-usability-improvements/plan.md)
- **Scope**: FE
- **Status**: In Progress

> 本ファイルは `plan.md` の「実装手順（Implementation Steps）」を  
> 実際の作業単位に分解した **2階層のチェックリスト** です。  
> AI コーディングや日々の実装は、この順序に沿って進めます。

## 1. 準備 / セットアップ

- [ ] リポジトリ / ブランチの準備
  - [ ] `feature/0011-mvp-usability-improvements` ブランチ作成
  - [ ] 対象ファイルと既存テストの確認
  - [ ] 判定UI/開始フロー/比率設定の現状挙動をメモ化

## 2. 実装タスク（Implementation Tasks）

### 次回PRスコープ（開始トリガー: 自動開始トグル）

- [ ] 自動開始トグル + 閾値保存の実装PRを作成
  - [x] 既存の10秒カウントダウンUI（残り秒数表示）を維持
  - [x] カウントダウン中でも手動開始ボタンを常時利用可能に維持
  - [x] `ViewController` に自動開始ON/OFFトグルUIを追加
  - [x] 閾値スライダーUI（最小/最大/現在値表示）を追加
  - [x] ON/OFFと閾値を `UserDefaults` に保存/読込
  - [x] countdown中のみ音量監視を有効化
  - [x] 閾値到達で `beginRecordingFromTrigger` を即時呼び出し
  - [x] タイムアウト時に手動開始誘導UIを表示
  - [x] 手動開始/カウントダウン満了/閾値到達の録画開始入口を1つに統一
  - [x] 開始イベントの二重発火ガードを確認
  - [ ] 手動確認（ON/OFF切替・再起動後保持・タイムアウト動作）

- [x] 判定UI改善（P1）
  - [x] `PoseImageView` のスコア表示を3段階（OK/注意/NG）で定義
  - [x] 要注意関節の強調表示（色/マーカー/ラベル）を追加
  - [x] 旧表示へ戻せる切替境界を追加
  - [x] 判定表示の境界値テストを追加

- [ ] 撮影開始トリガー改善（P1）
  - [x] 開始トリガー状態（`idle/countdown/recording`）を採用し、状態追加は行わない方針を明確化
  - [x] 録画開始前の10秒カウントダウンUI（残り秒数表示）を追加
  - [x] カウントダウン中でも手動開始ボタンを常時利用可能に維持
  - [x] 自動開始ON/OFFトグルと閾値設定の保持（`UserDefaults`）を追加
  - [x] 音量メータ値の簡易監視を追加し、閾値到達で即時開始
  - [x] 閾値未到達タイムアウト（例: 8秒）時は手動開始誘導UIへ切替
  - [x] 手動開始/カウントダウン満了/閾値到達の録画開始入口を1か所に統一
  - [x] 手動開始と自動開始の競合時に二重開始しないガードを追加
  - [x] 開始失敗時に手動開始へフォールバックする動線を確認

- [ ] 体格補正設定（P2）
  - [ ] 比率登録UI（初回キャリブレーション）を追加
  - [ ] 比率の保存/読込層（例: `UserDefaults` ラッパ）を実装
  - [ ] `TeacherStudentRatio` に保存値を適用
  - [ ] 未設定/破損時のデフォルト比率フォールバックを実装
  - [ ] 保存値反映の単体テストを追加

- [ ] 最小リファクタ（P3）
  - [ ] 開始トリガー状態管理の責務分離（`ViewController` から段階的に抽出）
  - [ ] 新規ロジックを責務ごとに分離（UI制御/トリガー判定/比率管理）
  - [ ] `ScaledPoseHelper` の重複処理を段階的に共通化
  - [ ] 既存機能回帰がないことを確認

## 3. テスト / 品質確認

- [ ] テストコードの整備
  - [ ] ユニットテスト（判定境界値 / 比率保存・読込）
  - [ ] ユニットテスト（開始トリガー状態遷移と二重開始ガード）
  - [ ] 必要なら結合テスト（撮影開始フロー）
- [ ] 手動確認
  - [ ] 判定UIが従来より理解しやすいことを確認
  - [ ] 手動開始/自動開始の両方で撮影できることを確認
  - [ ] 自動開始タイムアウト時に手動開始へ誘導されることを確認
  - [ ] 比率設定あり/なしで撮影と判定が破綻しないことを確認

## 4. ドキュメント / レビュー準備

- [ ] `impl-report.md` のドラフト作成
- [ ] `docs/specs/ui.md` / `docs/specs/structure.md` / `docs/specs/tech.md` の更新
- [ ] 比較スクリーンショット（改善前/改善後）を整理
- [ ] レビュアーに共有（Issue / PR にリンク）

## 5. マージ / アフターケア

- [ ] PR マージ
- [ ] リリースノート更新
- [ ] `impl-report.md` を `Done` に更新
- [ ] フォローアップ Issue の登録（音声認識高度化、全面リファクタ等）
